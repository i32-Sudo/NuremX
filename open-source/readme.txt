[Required Software]
Nvidia SDK
Python 3.9+ (With PIP and add to PATH)

[How To Run]
1. Open the (open-source) folder
2. install required files with PIP command
( pip install -r requirements.txt )
3. Once completed run the *EAC Bypass (RUN ME)* Shortcut
(NOT THE *EAC_Bypass.bat* FILE, ONLY SHORTCUT)
4. Done!